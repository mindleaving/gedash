<UserControl x:Class="CentralMonitorGUI.Views.WaveformView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CentralMonitorGUI.Views"
             xmlns:wpf="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             mc:Ignorable="d" 
             d:DesignHeight="80" d:DesignWidth="300"
             Name="Self"
             DataContext="{Binding ElementName=Self, Path=ViewModel}">
    <Grid Background="Aquamarine">
           <wpf:CartesianChart Series="{Binding WaveformSeries}">
               <wpf:CartesianChart.AxisX>
                   <wpf:Axis ShowLabels="False"/>
               </wpf:CartesianChart.AxisX>
               <wpf:CartesianChart.AxisY>
                   <wpf:Axis ShowLabels="False"/>
               </wpf:CartesianChart.AxisY>
        </wpf:CartesianChart>
    </Grid>
</UserControl>
